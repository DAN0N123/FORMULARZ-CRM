{
  "services": {
    "frontend": {
      "build": "cd frontend && npm install && npm run build",
      "start": "cd frontend && npx serve -s dist"
    },
    "backend": {
      "build": "cd backend && npm install",
      "start": "cd backend && npm start"
    }
  }
}
